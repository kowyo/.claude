To skip the onboarding process, add the following to `~/.claude.json`:

```json
{
  "hasCompletedOnboarding": true
}
```

## Installation

To download `settings.json` and replace your local version:

```bash
curl -o ~/.claude/settings.json https://raw.githubusercontent.com/kowyo/.claude/main/settings.json
```
